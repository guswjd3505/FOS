<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MemoActivity"></activity>
        <activity android:name=".MyPageActivity" />
        <activity android:name=".KBResActivity" />
        <activity android:name=".AResActivity" />
        <activity android:name=".HResActivity" />
        <activity android:name=".KBMenuActivity" />
        <activity android:name=".AMenuActivity" />
        <activity android:name=".HMenuActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ARimActivity" />
        <activity android:name=".KBActivity" />
        <activity android:name=".HamActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>